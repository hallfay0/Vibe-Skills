# Install Matrix

| Action | PowerShell | Shell | Result |
| --- | --- | --- | --- |
| Install | `install.ps1 -SkillsDir <path>` | `install.sh --skills-dir <path>` | `<SkillsDir>/vibe` |
| Check | `check.ps1 -SkillsDir <path>` | `check.sh --skills-dir <path>` | Checks the installed files |

## Remove

Delete `<SkillsDir>/vibe`.
